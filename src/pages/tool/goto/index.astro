---
import BaseHead from '../../../components/BaseHead.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../../../consts';


const speculationUrls = ['/', '/post', '/categories', '/tags', '/archives'];
---

<!doctype html>
<html lang="zh-CN" class="scroll-smooth">
  <head>
    <BaseHead
      title={`${SITE_TITLE} · URL 重定向`}
      description="安全的 URL 重定向工具，支持自定义协议"
      speculationUrls={speculationUrls}
      speculationEagerness="moderate"
      noindex={true}
    />
  </head>
  <body class="min-h-screen flex flex-col">
    <Header />
    <main class="flex-1 w-full max-w-4xl mx-auto px-4 sm:px-6 py-10 text-base leading-relaxed">
      <a href="/tool" class="no-style inline-flex items-center text-sm text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-200 transition-colors mb-6">
        <svg class="w-4 h-4 mr-1" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="15 18 9 12 15 6"></polyline>
        </svg>
        返回 Tools
      </a>

      <section class="mb-6">
        <h1 id="pageTitle" class="text-2xl sm:text-3xl font-semibold tracking-tight text-slate-900 dark:text-slate-100">
          URL 重定向
        </h1>
        <p id="pageDescription" class="mt-2 text-sm text-slate-600 dark:text-slate-400">
          安全的外链跳转工具，支持所有 URI 协议
        </p>
      </section>

      <div class="space-y-4 mb-6">
        <div id="countdownContainer" class="border border-slate-200 dark:border-slate-800 rounded-xl bg-white dark:bg-slate-900 p-6">
          <div class="text-center">
            <div class="inline-flex items-center justify-center mb-3">
              <span id="countdown" class="text-5xl font-semibold text-primary">3</span>
            </div>
            <p class="text-sm text-slate-700 dark:text-slate-300">秒后自动跳转</p>
          </div>
        </div>

        <div id="httpWarningContainer" class="hidden border border-red-200 dark:border-red-800 rounded-xl bg-red-50 dark:bg-red-900/20 p-6">
          <div class="flex items-start gap-3">
            <svg class="w-6 h-6 text-red-600 dark:text-red-400 shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
            <div class="flex-1">
              <h3 class="text-base font-semibold text-red-800 dark:text-red-200 mb-2">安全提醒</h3>
              <p class="text-sm text-red-700 dark:text-red-300 leading-relaxed">
                目标链接使用 <strong>HTTP</strong> 协议，可能存在安全风险。<br/>
                建议仅在可信环境下访问，避免在此页面输入敏感信息。<br/>
                <strong>出于安全考虑，HTTP 链接不会自动跳转。</strong>
              </p>
            </div>
          </div>
        </div>

        <div id="urlDisplayContainer" class="border border-slate-200 dark:border-slate-800 rounded-xl bg-white dark:bg-slate-900 p-5">
          <p class="text-xs font-medium text-slate-500 dark:text-slate-500 uppercase tracking-wide mb-2">目标网址</p>
          <p id="urlDisplay" class="text-sm text-primary break-all leading-relaxed">
            获取中...
          </p>
        </div>

        <div id="urlInputContainer" class="hidden space-y-4">
          <div class="border border-slate-200 dark:border-slate-800 rounded-xl bg-white dark:bg-slate-900 p-5">
            <label class="block">
              <span class="text-sm font-medium text-slate-900 dark:text-slate-100 mb-2 block">
                输入跳转地址
              </span>
              <input
                id="urlInput"
                type="text"
                placeholder="例如: https://github.com 或 steam://app/730"
                class="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-lg bg-white dark:bg-slate-900 text-slate-900 dark:text-slate-100 text-sm placeholder:text-slate-400 focus:outline-none focus:border-primary/60 focus:ring-2 focus:ring-primary/20 transition-colors"
              />
            </label>

            <div class="mt-4 space-y-2">
              <label class="flex items-center gap-2 cursor-pointer">
                <input
                  id="stopOption"
                  type="checkbox"
                  class="w-4 h-4 accent-primary rounded border-slate-300 dark:border-slate-600"
                />
                <span class="text-sm text-slate-700 dark:text-slate-300">不自动跳转 (stop=true)</span>
              </label>
              <label class="flex items-center gap-2 cursor-pointer">
                <input
                  id="isNewOption"
                  type="checkbox"
                  class="w-4 h-4 accent-primary rounded border-slate-300 dark:border-slate-600"
                />
                <span class="text-sm text-slate-700 dark:text-slate-300">立即跳转 (isNew=true)</span>
              </label>
            </div>
          </div>
        </div>
      </div>

      <div id="redirectButtonsContainer" class="flex flex-col sm:flex-row gap-3 justify-center items-center mb-6">
        <button
          id="redirectLink"
          class="no-style inline-flex items-center justify-center px-6 py-2.5 bg-primary text-white text-sm font-medium rounded-lg hover:bg-primary/90 active:scale-[0.98] transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/30 focus-visible:ring-offset-2 focus-visible:ring-offset-white dark:focus-visible:ring-offset-slate-950"
        >
          立即跳转
        </button>

        <button
          id="cancelBtn"
          class="no-style inline-flex items-center justify-center px-6 py-2.5 bg-slate-100 hover:bg-slate-200 dark:bg-slate-800 dark:hover:bg-slate-700 text-slate-900 dark:text-slate-100 text-sm font-medium rounded-lg active:scale-[0.98] transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/30 focus-visible:ring-offset-2 focus-visible:ring-offset-white dark:focus-visible:ring-offset-slate-950"
        >
          取消跳转
        </button>
      </div>

      <div id="inputButtonsContainer" class="hidden flex flex-col sm:flex-row gap-3 justify-center items-center mb-6">
        <button
          id="submitBtn"
          class="no-style inline-flex items-center justify-center px-6 py-2.5 bg-primary text-white text-sm font-medium rounded-lg hover:bg-primary/90 active:scale-[0.98] transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/30 focus-visible:ring-offset-2 focus-visible:ring-offset-white dark:focus-visible:ring-offset-slate-950"
        >
          确认跳转
        </button>

        <button
          id="copyBtn"
          class="no-style inline-flex items-center justify-center px-6 py-2.5 bg-slate-100 hover:bg-slate-200 dark:bg-slate-800 dark:hover:bg-slate-700 text-slate-900 dark:text-slate-100 text-sm font-medium rounded-lg active:scale-[0.98] transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/30 focus-visible:ring-offset-2 focus-visible:ring-offset-white dark:focus-visible:ring-offset-slate-950"
        >
          复制链接
        </button>
      </div>

      <div class="border border-slate-200 dark:border-slate-800 rounded-xl bg-white dark:bg-slate-900 p-5">
        <h3 class="text-xs font-medium text-slate-500 dark:text-slate-500 uppercase tracking-wide mb-3">
          使用说明
        </h3>
        <ul class="text-sm text-slate-600 dark:text-slate-400 space-y-2 list-none m-0 p-0">
          <li class="flex items-start gap-2">
            <span class="text-slate-400">•</span>
            <span>支持所有 URI 协议，如 https://、http://、steam://、csgo:// 等</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-slate-400">•</span>
            <span>stop=true：只显示跳转链接，不自动跳转</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-slate-400">•</span>
            <span>isNew=true：立即跳转，不等待倒计时</span>
          </li>
          <li class="flex items-start gap-2">
            <span class="text-slate-400">•</span>
            <span>出于安全考虑，所有外链跳转都会经过此确认页面</span>
          </li>
        </ul>
      </div>
    </main>
    <Footer />

    <script src="../../../scripts/goto.ts"></script>
  </body>
</html>
